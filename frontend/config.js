const CONFIG = {
    // UPDATE THIS URL AFTER DEPLOYING YOUR BACKEND (e.g., https://your-backend.onrender.com)
    // BACKEND_URL: 'https://backend-geniusca.online'
    BACKEND_URL:'http://127.0.0.1:3001'
};

// Make it available globally
window.API_BASE_URL = CONFIG.BACKEND_URL;
